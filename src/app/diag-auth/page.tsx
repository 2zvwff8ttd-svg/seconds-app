export default function DiagAuthPage() {
  return (
    <main style={{ padding: "1.5rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>diag-auth page fallback</h1>
      <p>
        通常は middleware が HTML を直返しするため、このページは表示されません。
      </p>
    </main>
  );
}
